<h1 class="text-center my-4">どの都市の商品を更新しますか？</h1>

<%= form_with url: edit_by_city_prices_path, method: :post, local: true, data: { turbo: false } do |form| %>

<div class="text-center my-8">
  <%= form.collection_select :city_id, @cities, :id, :name, prompt: "選択してください", required: true, class: "w-2/3 py-2 px-3 border rounded" %>
</div>

<div class="text-center">
  <%= form.submit "次に進む", class: "px-4 py-2 text-white bg-blue-500 rounded hover:bg-blue-600" %>
</div>

<% end %>
