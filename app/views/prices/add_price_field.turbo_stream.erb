<%= turbo_stream.update "flash", partial: "shared/flash_message" %>

<% if @price.present? %>
  <%= turbo_stream.append "price-fields", partial: "price_fields", locals: { price: @price } %>
<% end %>
