<!DOCTYPE html>
<html>

<head class="h-scroll scroll-smooth">
  <title><%= content_for(:title) || "Myapp" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="mobile-web-app-capable" content="yes">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= yield :head %>

  <link rel="manifest" href="/manifest.json">
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>

  <script src="https://kit.fontawesome.com/12b5d77523.js" crossorigin="anonymous"></script>
</head>

<body class="flex flex-col min-h-screen">
  <%= turbo_frame_tag "modal" %>

  <div class="flex-1">
    <header class="fixed top-0 left-0 w-full z-40">
      <%= render "shared/header" %>

    </header>
    <div id="flash" class="fixed top-16 left-0 w-full z-10">
      <%= render "shared/flash_message" %>
    </div>

    <main class="mt-20">
      <%= yield %>
    </main>
  </div>

  <footer class="w-full z-40 hidden sm:block">
    <%= render "shared/footer" %>
  </footer>
</body>

</html>
